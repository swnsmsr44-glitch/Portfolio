# Programmer Yassin — Portfolio

Files:
- index.html
- styles.css
- script.js

Usage:
1. Save the three files in the same folder.
2. Open `index.html` in your browser.

Features:
- Responsive single-page portfolio layout.
- Three themes: dark (default), light, and teal.
- Theme selection buttons in the header (stored in localStorage).
- Smooth transitions and smooth scroll for anchor links.
- Simple contact form (mailto) and placeholder project entries.

Customize:
- Replace project items and links inside `index.html`.
- Change the avatar initials or add an image in the hero card.
- Modify or add theme variables in `styles.css` (search for `html[data-theme="..."]`).

If you want, I can:
- Add images and icons (favicon, Open Graph).
- Prepare a GitHub Pages-ready repo and push the files for you.
- Translate the site content into Arabic (RTL) or make a language switcher.